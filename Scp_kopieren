#!/usr/bin/expect -f

spawn scp "Jufo@ip:/home/Jufo/datenbank.db" /home/Jufo/Audiosystem

expect {
  -re ".*es.*o.*" {
    exp_send "yes\r"
    exp_continue
  }
  -re ".*sword.*" {
    exp_send "PASSWORD\r"
  }
}
interact
